This project targets a player win probability near 50% because it represents a mathematically principled notion of “balanced challenge.” Modeling each game outcome as a Bernoulli trial with player win probability ppp, the outcome entropy H(p)H(p)H(p) is maximized at p=0.5p=0.5p=0.5, corresponding to the most unpredictable and balanced gameplay. Additionally, the variance p(1−p)p(1-p)p(1−p) is also maximized at 0.5, meaning win/loss outcomes are most informative for data-driven adaptation near this point. Framed as a feedback control problem, 0.5 provides a symmetric and interpretable setpoint: win rates above 0.5 indicate the AI is too weak, while rates below 0.5 indicate the AI is too strong. In practice, the system will aim to keep the win rate within a tolerance band (e.g., 45–55%) to account for sampling noise and player improvement over time.
